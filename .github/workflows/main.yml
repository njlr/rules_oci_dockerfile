name: Main

on:
  pull_request:
  merge_group:
  workflow_dispatch:

jobs:

  main:
    runs-on: ubuntu-22.04-16core

    steps:
      - uses: actions/checkout@v3

      - name: Bazel
        run: bazel test //...
