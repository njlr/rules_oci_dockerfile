name: Main

on:
  push:
  pull_request:
  merge_group:
  workflow_dispatch:

jobs:

  main:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v3

      - name: Bazel
        run: bazel test //...
