actions:
  - name: "Test all targets"
    triggers:
      push:
        branches:
          - "master"
      pull_request:
        branches:
          - "*"
    steps:
      - run: sudo apt-get update -y && sudo apt-get install -y libicu-dev
      - run: "bazel test //..."
