actions:
  - name: "Test all targets"
    triggers:
      push:
        branches:
          - "master"
      pull_request:
        branches:
          - "*"
    steps:
      - run: apt-get install -y libicu-dev
      - run: "bazel test //..."
